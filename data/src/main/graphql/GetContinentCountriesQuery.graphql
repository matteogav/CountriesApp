query GetContinentCountriesQuery($code: ID!) {
    continent(code: $code) {
        countries {
            name
            emojiU
            currency
            code
            phone
        }
    }
}